1. Added one more shell script that script will delete the old temp files form past 7 days. ==== Script Name == Cleanup.sh
2. Added one more shell script that script will checks if a service is running, and if not, start it. ====  Script Name === check_service.sh
                  Example: ./check_service.sh nginx
